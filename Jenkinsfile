pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'hello world'
                sh 'aws --version'
                input 'this is an input'
            }
        }
    }
}

